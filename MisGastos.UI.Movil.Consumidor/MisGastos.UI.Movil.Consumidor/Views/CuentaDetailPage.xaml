<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MisGastos.UI.Movil.Consumidor.Views.CuentaDetailPage"
             Title="{Binding Title}">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsEnabled="False"/>
    </Shell.BackButtonBehavior>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Guardar" Command="{Binding GuardarCuentaCommand}"/>
        <ToolbarItem Text="Eliminar" Command="{Binding EliminarCuentaCommand}" IsEnabled="{Binding HabilitarEliminar}"/>
    </ContentPage.ToolbarItems>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Text="Información cuenta:" Grid.Row="0"/>
        <Entry Placeholder="Nombre cuenta" Grid.Row="1" Text="{Binding Cuenta.Nombre}"/>
    </Grid>
</ContentPage>